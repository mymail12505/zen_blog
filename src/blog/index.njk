---
title: "Blog · 博客"
layout: layouts/base.njk
pagination:
  data: collections.blog
  size: 10
permalink: "/blog/"
---

<div class="container py-5">
  <h1 class="mb-4 text-center">📰 Blog · 博客</h1>
  <p class="text-center text-muted mb-5">
    Reflections, Zen insights, and life experiences.
  </p>

  <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    {% for post in pagination.items %}
    <div class="col">
      <article class="card h-100 shadow-sm border-0">
        {% if post.data.image %}
          <img src="{{ post.data.image }}" alt="{{ post.data.title }}" class="card-img-top rounded-top">
        {% endif %}
        <div class="card-body d-flex flex-column">
          <h5 class="card-title">{{ post.data.title }}</h5>
          <p class="card-text text-muted small mb-3">{{ post.data.excerpt }}</p>
          <a href="{{ post.url }}" class="btn btn-outline-dark mt-auto">阅读全文 · Read More</a>
        </div>
      </article>
    </div>
    {% endfor %}
  </div>

  {% if pagination.href.next %}
  <div class="text-center mt-4">
    <a href="{{ pagination.href.next }}" class="btn btn-outline-secondary">Next Page →</a>
  </div>
  {% endif %}
</div>
