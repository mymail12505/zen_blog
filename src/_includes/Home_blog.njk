<section class="py-5">
  <h2 class="mb-4 text-center">🪷 Latest Zen Reflections</h2>

  <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    {% for post in collections.blog | limit(3) %}
      <div class="col">
        <article class="card h-100 shadow-sm border-0">
          {% if post.data.image %}
            <img src="{{ post.data.image }}" alt="{{ post.data.title }}" class="card-img-top rounded-top">
          {% endif %}
          <div class="card-body d-flex flex-column">
            <h5 class="card-title">{{ post.data.title }}</h5>
            <p class="card-text text-muted small mb-3">
              {{ post.templateContent | striptags | truncate(100) }}
            </p>
            <a href="{{ post.url }}" class="btn btn-outline-dark mt-auto">阅读全文 · Read More</a>
          </div>
        </article>
      </div>
    {% endfor %}
  </div>

  <div class="text-center mt-4">
    <a href="/blog/" class="btn btn-outline-secondary">View All Blog Posts →</a>
  </div>
</section>
